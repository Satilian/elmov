{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn && lerna run start",
    "prod": "yarn && lerna run prod",
    "nx": "nx",
    "graph": "nx graph"
  },
  "devDependencies": {
    "lerna": "^5.1.6",
    "nx": "^14.4.0"
  }
}
