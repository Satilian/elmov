{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "yarn && lerna bootstrap",
    "start": "yarn bootstrap && lerna run start",
    "prod": "yarn bootstrap && lerna run prod",
    "nx": "nx",
    "graph": "nx graph"
  },
  "devDependencies": {
    "lerna": "^5.1.6",
    "nx": "^14.4.0"
  }
}
